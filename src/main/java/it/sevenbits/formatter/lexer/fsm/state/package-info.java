/**
 * This package contains interface that declares functionality for class that provides
 * mapping of transitions between FSM states as well as it's implementation.
 * <p>
 * Transition choice performs using information about current FSM state and other input signals.
 * <p>
 * Also this package contains various components that are necessary for class work
 * (including class that actually presents FSM state).
 *
 * @author hu553in
 */
package it.sevenbits.formatter.lexer.fsm.state;
