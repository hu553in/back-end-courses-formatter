language: java
jdk:
  - openjdk8
sudo: false

cache:
  directories:
  - $HOME/.m2

variables:
  MAVEN_CLI_OPTS: "--batch-mode --errors --fail-at-end --show-version -DinstallAtEnd=true"

install: true
script: "mvn $MAVEN_CLI_OPTS clean install"
